[project]
name = "image-processor"
version = "0.1.0"
description = "An MCP server for advanced image processing, including AI-powered background removal, resizing, and format conversion."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.13.1",
    "requests>=2.31.0",
    "pillow>=10.0.0",
    "rembg>=2.0.0",
    "onnxruntime>=1.15.0",
]
